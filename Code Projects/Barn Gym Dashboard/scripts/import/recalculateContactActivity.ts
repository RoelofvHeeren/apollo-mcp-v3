import { logStep } from "./utils";

export async function recalculateContactActivity() {
  logStep("recalculateContactActivity placeholder");
  // TODO: Update isActive / lastPaymentAt / defaults based on transaction timestamps
}
