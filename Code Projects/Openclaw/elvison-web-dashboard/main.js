document.addEventListener('DOMContentLoaded', () => {
    // Baseline initialization
    if (window.lucide) {
        lucide.createIcons();
    }
    console.log("Mission Control Baseline Ready.");
});
